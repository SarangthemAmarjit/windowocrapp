set mainpage index

4 permit generated after cash

7 permit generated if there is existing user

6 if online payment failed to process
5 if paym


import 'package:flutter/material.dart';
import 'package:flutter_inappwebview/flutter_inappwebview.dart';

class WebViewPage extends StatefulWidget {
  @override
  _WebViewPageState createState() => _WebViewPageState();
}

class _WebViewPageState extends State<WebViewPage> {
  final GlobalKey webViewKey = GlobalKey();
  InAppWebViewController? webViewController;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("WebView with Input Fix (Windows)")),
      body: InAppWebView(
        key: webViewKey,
        initialUrlRequest: URLRequest(url: Uri.parse("https://yourwebsite.com")),
        onWebViewCreated: (controller) {
          webViewController = controller;
        },
        onLoadStop: (controller, url) async {
          // Fix 1: Ensure input fields always keep focus
          await controller.evaluateJavascript(source: """
            document.addEventListener('mousedown', function(event) {
              if (event.target.tagName === 'INPUT' || event.target.tagName === 'TEXTAREA') {
                event.target.focus();
              }
            });
          """);

          // Fix 2: Detect input focus and force it back
          await controller.evaluateJavascript(source: """
            document.querySelectorAll('input, textarea').forEach(element => {
              element.addEventListener('focus', () => {
                window.console.log('INPUT_FOCUS');
              });
            });
          """);

          // Fix 3: Prevent losing focus due to clicks outside
          await controller.evaluateJavascript(source: """
            document.body.addEventListener('mousedown', function(event) {
              if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                event.preventDefault();
              }
            });
          """);
        },
        onConsoleMessage: (controller, consoleMessage) {
          if (consoleMessage.message == "INPUT_FOCUS") {
            controller.evaluateJavascript(source: "document.activeElement.focus();");
          }
        },
      ),
    );
  }
}


import 'package:flutter/material.dart';
import 'package:flutter_inappwebview/flutter_inappwebview.dart';

class WebViewPage extends StatefulWidget {
  @override
  _WebViewPageState createState() => _WebViewPageState();
}

class _WebViewPageState extends State<WebViewPage> {
  final GlobalKey webViewKey = GlobalKey();
  InAppWebViewController? webViewController;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("WebView with Touch Input Fix (Windows)")),
      body: InAppWebView(
        key: webViewKey,
        initialUrlRequest: URLRequest(url: Uri.parse("https://yourwebsite.com")),
        onWebViewCreated: (controller) {
          webViewController = controller;
        },
        onLoadStop: (controller, url) async {
          // Fix 1: Ensure input fields always keep focus when touched
          await controller.evaluateJavascript(source: """
            document.addEventListener('touchstart', function(event) {
              if (event.target.tagName === 'INPUT' || event.target.tagName === 'TEXTAREA') {
                event.target.focus();
              }
            });
          """);

          // Fix 2: Prevent losing focus due to clicking outside inputs
          await controller.evaluateJavascript(source: """
            document.body.addEventListener('pointerdown', function(event) {
              if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                event.preventDefault();
              }
            });
          """);

          // Fix 3: Detect input focus and force it back
          await controller.evaluateJavascript(source: """
            document.querySelectorAll('input, textarea').forEach(element => {
              element.addEventListener('focus', () => {
                window.console.log('INPUT_FOCUS');
              });
            });
          """);
        },
        onConsoleMessage: (controller, consoleMessage) {
          if (consoleMessage.message == "INPUT_FOCUS") {
            controller.evaluateJavascript(source: "document.activeElement.focus();");
          }
        },
      ),
    );
  }
}

